# QR Code Main

Minimal scaffold matching requested structure. Fill in logic as needed.

## Scripts

- `npm start` — run Express server on `PORT` (default 3000)

## Env

Copy `.env` and adjust values.

